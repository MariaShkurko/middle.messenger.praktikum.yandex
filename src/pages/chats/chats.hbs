<div class="chats">
  <aside class="chats__sidebar">
    <div class="chats__header">
      {{> Button label="Профиль" class="chats__button-link" variant="link" page="user-profile"}}
      {{> Input id="search" label="Поиск" type="text" variant="outlined" leftIcon=searchIcon}}
    </div>
    <div class="chats__list">
      {{#each data.chats}}
        {{> ChatItem this}}
      {{/each}}
    </div>
  </aside>
  <main class="chats__main">
    {{#if showDialog}}
      {{> CorrespondenceFeed contact=data.selectedChat messages=data.messages arrowIcon=arrowIcon }}
    {{else}}
      {{> EmptyFeed }}
    {{/if}}
  </main>
</div>